# Cross-compiled by cargo-ndk on every build; never a source artifact.
app/src/main/jniLibs/
# Gradle's own output and caches.
.gradle/
build/
app/build/
# Generated by `gradle wrapper`, not committed so the repository holds no
# binary jar.
gradlew
gradlew.bat
gradle/wrapper/gradle-wrapper.jar
local.properties
